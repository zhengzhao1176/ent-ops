# Node / npm
node_modules/
.npm/
*.log
npm-debug.log*

# OS / editor / IDE
.DS_Store
.idea/
.vscode/
*.swp
*~

# Claude Code project state
.claude/

# playwright-cli scratch (per-command page snapshots / console logs)
.playwright-cli/

# Next.js / React build artifacts
.next/
out/
.vercel/
.open-next/

# Cloudflare wrangler local state
.wrangler/
*.dev.vars

# Local databases (SQLite + WAL/SHM/journal)
*.db
*.db-journal
*.db-shm
*.db-wal

# Test artifacts
coverage/
.test-state/
playwright-report/
playwright/.cache/
test-results/
.playwright-cli/

# Reports — keep dirs but ignore generated content
server/reports/runs/
server/reports/failures/
server/reports/history/
server/reports/latest
server/reports/playwright-html/
server/reports/playwright-traces/
server/reports/walker-screenshots/
server/reports/walker-cli/
server/reports/prod-walk/
!server/reports/.gitkeep
!server/reports/**/.gitkeep

# Env / secrets
.env
.env.local
.env.*.local
# Keep .env.example as a template
!.env.example

# TypeScript build info
*.tsbuildinfo

# Prisma generated client (regenerated by `prisma generate`)
node_modules/@prisma/client/

# Misc
tmp/
