# Local development env (copy to .env, do NOT commit .env)
# Production secrets are set via `wrangler pages secret put` — see DEPLOY.md.

DATABASE_URL="file:./dev.db"
JWT_SECRET="generate-a-32+-char-random-string-here"
NODE_ENV="development"
